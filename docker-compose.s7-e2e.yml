services:
  app:
    build:
      context: .
      target: e2e-runner
    env_file:
      - ${COMPOSE_ENV_FILE:-.env.s7-e2e}
    ports: !override
      - "127.0.0.1:${S7_E2E_APP_PORT:-3003}:3000"
    environment:
      UPLOAD_DIR: ${UPLOAD_DIR:-/app/uploads/e2e-nested}
